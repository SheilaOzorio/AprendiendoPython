import math
import random
print(math.pow(10 , 2))
print(math.sqrt(50))
print(math.isqrt(50))
print(math.sin(90))
print(math.cos(60))
print(math.tan(90))
print(math.factorial(5))

print(random.randint(15, 100))
# SIEMPRE  es necesario llamar a la libreria con import al de esa forma tendremos disponibles sus funciones sin que arroje error como resultado
# math nos sirve para empezar a llamar funciones de la librebria de matematicas
# .pow eleva un número a un exponente (raíz cuadrada, cubica)
# .sqrt saca la raíz cuadrada de cualquier número
# .isqrt saca la ra´zi cuadrada en entero
# .sin  seno: que es la razón entre el cateto upuesto al ángulo y la hipotenusa
# .cos coseno: es el cociente entre la longitud del cateto contiguo al al ángulo a y la longitud de la hipotenusa
# .tan tangente: que es la línea recta que sólo tiene un punto en común con una curva. 
# .factorial saca el factorial de un número. que es el número multiplicado por todos los números antes de él como el 5, sería  5!=5*4*3*2*1=120
# .random.randint, es parte de otra libreria que es random, esta funcion 'randint' escoge un número al azar de un rango de número que le proporciones